// Prisma schema — Petzo
// Models de domínio serão adicionados na Fase 1 (auth/users) em diante.

generator client {
  provider      = "prisma-client-js"
  binaryTargets = ["native", "linux-musl-openssl-3.0.x"]
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
